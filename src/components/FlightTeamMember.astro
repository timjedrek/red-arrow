---
const { teamMember } = Astro.props;
---

<div
  class="flex flex-col p-5 gap-5 rounded-lg shadow-lg shadow-gray-500 text-black bg-gradient-to-bl from-gray-200 to-gray-300 lg:last:col-span-2"
>
  <div class="flex flex-col rounded-lg gap-3 max-w-md">
    <img
      src={teamMember.image}
      class="aspect-square object-cover w-full rounded-lg border-2 border-black bg-white"
      alt={teamMember.alt}
    />
    <div
      class="p-3 rounded-lg text-black bg-transparent border-2 border-main-red"
    >
      <h3 class="text-xl font-semibold">{teamMember.name}</h3>
      <p class="italic">{teamMember.title}</p>
      <p class="text-sm">{teamMember.subtitle}</p>
    </div>
  </div>
  <div class="max-w-md flex flex-col justify-between h-full gap-2">
    <div class="flex flex-col gap-2">
      {teamMember.bio.map((paragraph) => <p class="text-sm">{paragraph}</p>)}
    </div>
    <div>
      <p class="text-end font-semibold text-main-red">{teamMember.email}</p>
      <p class="text-end font-semibold">{teamMember.phone}</p>
    </div>
  </div>
</div>
