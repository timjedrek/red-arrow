---
import ContactUs from "../../components/ContactUs.astro";
import BaseLayout from "../../layouts/BaseLayout.astro";
import TeamMemberCard from "../../components/TeamMemberCard.astro";
import { leadershipTeam, flightTeam } from "../../data/team.js";
import Location from "../../components/Location.astro";
import EnrollNowCTA from "../../components/EnrollNowCTA.astro";
import FlightTeamMember from "../../components/FlightTeamMember.astro";
---

<BaseLayout
  pageTitle="Our Team | Red Arrow Flight Academy"
  pageDescription="Meet our expert and caring team ready to take you to new heights. Get to know our leadership and dispatch teams at Red Arrow. See who you’ll be flying with!"
  pageKeywords="Team, staff, Red Arrow, leadership, dispatch, CFII, CFI, pilot, training"
>
  <header
    class="relative h-screen-3/4 bg-our-team bg-cover bg-center bg-black flex flex-col items-center justify-center"
  >
    <div class="absolute bg-black/50 w-full h-full z-0"></div>
    <div
      class="text-white text-center z-10 flex flex-col items-center gap-4 px-5"
    >
      <h1 class="text-5xl font-bold tracking-tighter">
        The People Behind Red Arrow
      </h1>
      <h2 class="text-3xl tracking-wide">
        Our elite flight instruction team in Santa Teresa, NM
      </h2>
      <a href="#contactUs" class="btn-red">Meet Red Arrow</a>
    </div>
  </header>

  <section
    class="flex flex-col items-center px-10 bg-gradient-to-br from-white to-gray-200 py-10"
  >
    <h2 class="text-4xl font-semibold text-center tracking-tight">
      Leadership Team
    </h2>

    {leadershipTeam.map((member) => <TeamMemberCard teamMember={member} />)}
    <hr class="bg-black w-full h-2 rounded-xl max-w-4xl my-14" />
    <h2 class="text-4xl font-semibold text-center tracking-tight">
      Flight Team
    </h2>

    <div
      class="grid grid-cols-1 lg:grid-cols-2 gap-5 py-10 justify-items-center"
    >
      {flightTeam.map((member) => <FlightTeamMember teamMember={member} />)}
    </div>

    <a href="/flight-programs/training-courses" class="">
      <div class="btn-red">Get in Touch</div>
    </a>
  </section>

  <Location />

  <EnrollNowCTA />

  <ContactUs />
</BaseLayout>
